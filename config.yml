projects:
  - name: sample
    token: really-gud-secret
    env:
      LOG: /tmp/sample.log
    commands:
      - "echo $LOG"
      - "sleep 5"
      - "touch $LOG"
      - "echo write to file"
      - "echo test >> $LOG"
      - echo done
